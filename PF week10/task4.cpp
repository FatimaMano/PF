#include <iostream>
using namespace std;
int ArrayNumbers[50];
bool checkNumber(int number, int number1, int number2);
main()
{
    int count = 0;
    int arraysize;
    cout << "Array Length";
    cin >> arraysize;
    int cyclelength;
    cout<<"Enter cycle length";
    cin>>cyclelength;
    int numbers[arraysize];
    for (int idx = 0; idx < arraysize; idx++)
    {
        cout << "Enter number";
        cin >> numbers[idx];
    }
    for (int idx = 0; idx < arraysize; idx++)
    {
    }
}